/**
 * @format
 */

import {AppRegistry} from 'react-native';
import MainActivity from './App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => MainActivity);
